#include "main.h"

/**
  *int_strlen - funtcion that returns the length of a string
  *@s: Char
  *Return: Always 0
  */

int_strlen(char *s)
{
	int i;

	i = 0;
	while (s[i] != '\0')
	{
		i++;
	}
	return (0);
}
